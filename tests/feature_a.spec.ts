import { test, expect } from "@playwright/test";

test.describe("Pool A: FR-01 - Account Registration", () => {
  test("Dummy test để khởi tạo dự án", async ({ page }) => {
    expect(true).toBeTruthy();
  });
});
