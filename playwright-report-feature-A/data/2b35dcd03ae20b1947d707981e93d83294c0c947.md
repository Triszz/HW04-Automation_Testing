# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: feature_a.spec.ts >> Pool A - FR-01: Account Registration >> [TC07] Negative - Register with email: test7@domain.com
- Location: tests\feature_a.spec.ts:8:9

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('.bg-red-100')
Expected substring: "Mật khẩu phải chứa ít nhất 1 chữ thường"
Received string:    "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT."
Timeout: 5000ms

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('.bg-red-100')
    14 × locator resolved to <div class="bg-red-100 text-red-700 p-3 mb-4 rounded">Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm…</div>
       - unexpected value "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT."

```

```yaml
- text: Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT.
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | import registerData from "../data/data_feature_a.json";
  3  | 
  4  | const BASE_URL = "http://localhost:5173/register";
  5  | 
  6  | test.describe("Pool A - FR-01: Account Registration", () => {
  7  |   for (const data of registerData) {
  8  |     test(`[${data.id}] ${data.type} - Register with email: ${data.email}`, async ({
  9  |       page,
  10 |     }) => {
  11 |       await page.goto(BASE_URL);
  12 | 
  13 |       // 1. Selector bằng CSS Adjacent Sibling rất mạnh mẽ (không cần id hay name)
  14 |       await page.locator('label:has-text("Họ Tên") + input').fill(data.name);
  15 |       await page.locator('label:has-text("Email") + input').fill(data.email);
  16 |       await page.locator('input[type="password"]').fill(data.password);
  17 | 
  18 |       // await page.locator('label:has-text("Xác nhận mật khẩu") + input').fill(data.confirmPassword);
  19 | 
  20 |       // Click nút bằng getByRole (tiêu chuẩn cao nhất)
  21 |       await page.getByRole("button", { name: "Đăng Ký" }).click();
  22 | 
  23 |       // 2. Assertions
  24 |       if (data.expectedResult === "Success") {
  25 |         // Assertion Pattern 1: Kiểm tra trạng thái điều hướng của trang (Page State/URL Assertion)
  26 |         await expect(page).toHaveURL(/.*login/);
  27 |       } else {
  28 |         // Lấy thẻ div chứa thông báo lỗi (dựa vào class bg-red-100 từ React code)
  29 |         const errorMsg = page.locator(".bg-red-100");
  30 |         // Assertion Pattern 2: Kiểm tra trạng thái hiển thị của DOM (Visibility Assertion)
  31 |         await expect(errorMsg).toBeVisible();
  32 |         // Assertion Pattern 3: Kiểm tra nội dung văn bản (Text Content Assertion)
> 33 |         await expect(errorMsg).toContainText(data.expectedMessage, {
     |                                ^ Error: expect(locator).toContainText(expected) failed
  34 |           ignoreCase: true,
  35 |         });
  36 |       }
  37 |     });
  38 |   }
  39 | });
  40 | 
```